apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argo-workflows-resources
spec:
  destination:
    namespace: argo
    server: 'https://kubernetes.default.svc'
  source:
    path: apps/argo-workflows-resources
    repoURL: 'git@github.com:jyje/r4spi-app-deployment.git'
    targetRevision: develop
  project: default
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated: {}
